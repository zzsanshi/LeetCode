#include<vector>
#include<iostream>
#include<stack>
#include<array>
using namespace std;
// TODO
// void backtrack(vector<bool> &clo,vector<bool> &l,vector<bool> &r,vector<vector<string>> &out,vector<string> &tem,int pos,int n){
//     if(pos==n){
//         out.emplace_back(tem);
//     }
//     for()
// }

vector<vector<string>> solveNQueens(int n) {
    vector<bool> column(n,false);
    vector<bool> l(n,false);
    vector<bool> r(n,false);
    vector<vector<string>> out;
    vector<string> tem;
    return out;
}